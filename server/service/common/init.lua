
return {
    -- 匹配
    match_compete = function(agent, wait_room)

    end,

    -- 排位
    rank_compete = function(agent, wait_room)

    end,

    -- 自定义
    custom_compete = function(agent, custom_room)

    end
}